import { Opts, Presets } from './interfaces';
declare const presets: Presets;
declare function validateMessage(message: string, options?: Opts): boolean;
declare function validateMessageFromFile(file: string, options?: Opts): boolean;
export default presets;
export { validateMessage, validateMessageFromFile };
